/** @type {import('next').NextConfig} */
const nextConfig = {
    // Define publicRuntimeConfig for runtime configuration
    publicRuntimeConfig: {
      // Add your path or any other configuration
      basePath: 'pages/index',
    },
  
    // You can add more configurations here based on your requirements
  }
  
  module.exports = nextConfig;  